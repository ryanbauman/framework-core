machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t redhawkci/redhawk .

#test:
#  override:
#    - true

#deployment:
#  hub:
#    branch: circle
#    commands:
#       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#       - docker push redhawkci/redhawk
